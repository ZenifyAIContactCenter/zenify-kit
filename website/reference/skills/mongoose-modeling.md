---
title: /mongoose-modeling
---

# `/mongoose-modeling`

Use when designing or changing a Mongoose schema — schema/index design, lean vs populate, discriminators, and the safe order to add a new field (optional → switch writers → backfill → require).

## Cách gọi

```text
/mongoose-modeling
```

## Tool được phép

`Read Grep Glob Bash`

## Nguồn

Sinh bởi `zenify docs gen` từ frontmatter `SKILL.md` trong binary; phần thân skill là agent-read, không hiển thị ở đây.
