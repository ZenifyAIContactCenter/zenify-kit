---
title: /react-patterns
---

# `/react-patterns`

Use when writing or changing React (Vite/TanStack) frontend code — component/state structure, list/table rendering gated on a total count, controlled forms, axios interceptor + timeout that exempts blob/arraybuffer downloads, ICU single-brace i18n, and env-driven feature gating.

## Cách gọi

```text
/react-patterns
```

## Tool được phép

`Read Grep Glob Bash`

<!-- Generated by zenify docs gen from the SKILL.md frontmatter embedded in the binary; the skill body is agent-read and not rendered. -->
