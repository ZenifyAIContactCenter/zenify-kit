---
title: zenify observe statusline install
---

## zenify observe statusline install

Ghi key statusLine → `zenify observe statusline` vào ~/.claude/settings.json (chỉ khi trống)

```
zenify observe statusline install [flags]
```

### Options

```
      --force   đè statusLine hiện có (mặc định từ chối nếu đã có cái khác)
  -h, --help    help for install
```

### SEE ALSO

* [zenify observe statusline](./zenify_observe_statusline)	 - HUD statusline: hiện model · ctx% · ⟳dispatch · ↓tool-output · $cost

